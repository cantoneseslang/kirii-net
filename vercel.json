{
  "installCommand": "corepack enable && corepack prepare pnpm@10.18.0 --activate && cd kirii-net && pnpm install --frozen-lockfile",
  "buildCommand": "cd kirii-net && pnpm run build",
  "framework": "nextjs"
}
